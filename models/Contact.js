class Contact {
    constructor(id, name, mobile, landLine, imageUri) {
      this.id = id;
      this.name = name;
      this.mobile = mobile;
      this.landLine = landLine;
      this.imageUri = imageUri;
      // this.imageUri = require('../images/icon_user.png');
    }
  }
  
  export default Contact;
  